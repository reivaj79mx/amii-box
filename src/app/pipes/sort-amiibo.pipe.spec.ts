import { SortAmiiboPipe } from './sort-amiibo.pipe';

describe('SortAmiiboPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAmiiboPipe();
    expect(pipe).toBeTruthy();
  });
});
